//codigo para leer la id en un rc552 en python
//POSIBLES AMPLIACIONES: 
  //identificar el id, tipo "id:###"
  //posibiliad de editr la infromación del aparato. decidir el id y el texto que quiero
 //CUESTIONES
  //la placa del parking es igual pero no funciona, pq?
#!/usr/bin/env python

import RPi.GPIO as GPIO
from mfrc522 import SimpleMFRC522

reader = SimpleMFRC522()

try:
        id, text = reader.read()
        print(id)
        print(text)
finally:
        GPIO.cleanup()
